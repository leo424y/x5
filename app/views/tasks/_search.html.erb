<%= form_for :task, html: {method: :get} do |f| %>
  <%= f.label 'content', style: "display:none;" %>
  <%= f.search_field :content, autofocus: true %>
  <%= f.label 'state', style: "display:none;" %>
  <%= f.select :state, ['',t('todo'), t('doing'), t('done')] %>
  <%= f.submit t('search') %>
<% end %>